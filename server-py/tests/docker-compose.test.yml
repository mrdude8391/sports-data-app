services:
  postgres:
    image: postgres:latest
    container_name: sports-app-test-postgres
    environment:
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpass
      POSTGRES_DB: sports-test
    ports:
      - "5432:5432"
